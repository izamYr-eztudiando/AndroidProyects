<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_gravity="center_horizontal"
    tools:context=".Menu">

    <!-- TODO: Update blank fragment layout -->
    <LinearLayout

        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal"
        android:weightSum="1">

    <ImageButton android:id="@+id/linterna" style="@style/botones" android:src="@drawable/linterna"/>
    <ImageButton android:id="@+id/nivel" style="@style/botones" android:src="@drawable/nivel"/>
    <ImageButton android:id="@+id/musica" style="@style/botones" android:src="@drawable/musica"/>

    </LinearLayout>


</RelativeLayout>