<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context=".Menu"
    android:layout_gravity="center_horizontal">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal"
        android:weightSum="1">

        <ImageButton android:id="@+id/linterna"
            style="@style/botones"
            android:src="@drawable/linterna"/>

        <ImageButton android:id="@+id/nivel"
            style="@style/botones"
            android:src="@drawable/nivel"/>

        <ImageButton android:id="@+id/musica"
            style="@style/botones"
            android:src="@drawable/musica"/>

        <ImageButton android:id="@+id/vibracion"
            style="@style/botones"
            android:src="@drawable/vibracion_off"/>

        <ImageButton android:id="@+id/imgHuella"
            style="@style/botones"
            android:src="@drawable/huella_off"/>

        <ImageButton android:id="@+id/grabadora"
            style="@style/botones"
            android:src="@drawable/grabadora"/>

    </LinearLayout>

</RelativeLayout >